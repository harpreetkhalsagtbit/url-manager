import React from 'react';
import PropTypes from 'prop-types'; // ES6
import {TextArea } from 'semantic-ui-react'

const Test = ({name, label, onChange, rows, placeHolder, value="", error}) => {

	return (
		<div className="">
		</div>

	);
};

export default Test;

